echo building image...
docker image build -t server:v2 .

echo removing existing docker...
docker rm -f server-1

echo running docker container from the above image...
docker run  --link some-zookeeper:server-1 -d --name server-1 -t "server:v2" 
